<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_student_form" model="ir.ui.view">
        <field name="name">view.student.form</field>
        <field name="model">student</field> 
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <group>
                            <field name="name" placeholder="Nombre"></field>
                            <field name="grade" placeholder="Grado"></field>
                        </group>
                        <group>
                            <field name="age" placeholder="Edad"></field>
                            <field name="student_number" placeholder="Número de alumno" widget="integer" options="{'format': 0}"></field>
                        </group>
                    </group>
                </sheet>
                </form>
                </field>
    </record>

</odoo>
